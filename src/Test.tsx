import React from 'react'

const last = (arr: Array<number>) => {
  return arr[arr.length - 1];
}

const l = last([1, 2, 3]);


function Test() {
  return (
    <div>
      
    </div>
  )
}

export default Test
